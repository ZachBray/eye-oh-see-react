{
  "name": "eye-oh-see-react",
  "version": "0.0.1",
  "main": "dist/Index.js",
  "typings": "dist/Index.d.ts",
  "scripts": {
    "clean": "rimraf dist",
    "lint": "tslint ./**/*.ts",
    "pretest": "yarn run lint",
    "test": "echo TODO write tests",
    "prebuild": "yarn run clean && yarn run test",
    "build": "tsc --outDir ./dist -p . ",
    "precommit": "yarn run build"
  },
  "devDependencies": {
    "@types/react": "16.x",
    "@types/rx": "^4.x",
    "eye-oh-see": "0.5.x",
    "rimraf": "2.6.1",
    "tslint": "5.7.0",
    "typescript": "2.4.2"
  },
  "peerDependencies": {
    "eye-oh-see": "0.5.x",
    "react": "15.x",
    "rx": "4.x"
  }
}
